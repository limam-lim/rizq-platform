# تعليمات ثابتة من Limam (محفوظة — لا تُطرح كسؤال مرة أخرى)

**تاريخ الحفظ:** 22/07/2026

## القاعدة
عندما يتم اكتشاف أي إصلاح مطلوب مستقبلاً في منصة رزق (ثغرة أمنية، خلل، تعارض بين الصفحات، إلخ) أثناء أي تدقيق أو مراجعة لاحقة — **يجب تنفيذ الإصلاح مباشرة دون طلب الإذن أولاً**، ثم إخبار Limam بما تم إصلاحه وأين.

هذا لا يشمل: حذف بيانات نهائياً، إرسال رسائل نيابة عنه، أو أي عملية تتطلب إذناً صريحاً بحكم القواعد العامة للأمان (مثل المدفوعات أو نشر محتوى علني) — تلك تبقى تُطرح كسؤال كالمعتاد.

## أول تطبيق لهذه القاعدة
ثغرة `rizq_admin.html` (كلمات سر صريحة في `ADMIN_ACCOUNTS` + تسجيل دخول وهمي بلا تحقق من الخادم) — تم إصلاحها مباشرة في نفس الجلسة التي اكتُشفت فيها، بدون انتظار موافقة على كل خطوة.

## قاعدة ثانية — إلزامية في بداية كل جلسة عمل قادمة
**أول شيء يجب فعله عند بدء أي جلسة عمل جديدة على هذا المشروع هو مراجعة أمنية سريعة للتأكد من عدم وجود أي ثغرة في المنصة** (تحقق من: مصادقة `rizq_admin.html` لا تزال حقيقية عبر الخادم ولم تُعَد إلى نمط محلي، عدم وجود كلمات سر/مفاتيح API صريحة جديدة في أي ملف HTML/JS يصل للمتصفح، `BACKEND_SHARED_SECRET` لا يزال غير مخزَّن إلا في متصفح الأدمن، أي endpoints جديدة أُضيفت محمية بـ`requireSharedSecret` أو مصادقة مكافئة، عدم وجود XSS جديد عبر إدخال مستخدم غير مُهرَّب في `innerHTML`). هذا يتم **قبل** البدء بأي طلب آخر من Limam في تلك الجلسة، وبشكل تلقائي دون انتظار أن يطلبها — ثم إخباره بالنتيجة (سليم / وُجد كذا وتم إصلاحه) باختصار قبل المتابعة لبقية طلبه.

## سجل الجلسة الذاتية (22/07/2026 — أثناء غياب Limam في العمل)
تنفيذاً للأمر "واصل انت بدون توقف فقط الاصلاحات والمراجعة وتسرب"، تمت مراجعة كل الملفات المعدَّلة اليوم + مسح شامل للمنصة، ووُجد وأُصلح مباشرة:
1. **تسرّب لغوي** في `rizq_landing_v8.html`: قائمة فئة النشاط التجاري (3 نماذج تسجيل: محل/مكتب/شركة) كانت ترجمتها الفرنسية تحتوي كلمة عربية "معارض" — أُصلحت إلى "showrooms".
2. **تسرّب لغوي** في `rizq_browse.html`: زر "رجوع للقائمة" في صفحة تفاصيل الإعلان كان يعرض "الإعلانات" بالعربية حتى في الوضع الفرنسي (الترجمة لم تُكتب أصلاً) — أُصلحت إلى "Annonces".
3. **ثغرة XSS انعكاسية حقيقية** في `rizq_search.html`: قيمتا `?cat=` و`?sub=` من رابط الصفحة كانتا تُدرجان مباشرة (بلا تهريب) عبر `innerHTML` في شريط "نتائج القسم" — رابط مُعدّ خصيصاً كان يمكنه تنفيذ كود في متصفح الزائر. أُصلحت بتمرير القيمتين عبر `escapeHtml()` الموجودة أصلاً في نفس الملف.
4. تحسين بسيط (ليس ثغرة): `rizq_showroom_directory.html` كانت تعرض "0 نتيجة" للحظة قبل وصول بيانات الخادم — أُضيف مؤشر تحميل بدلاً من ذلك.
تم أيضاً مسح شامل للمنصة بحثاً عن نفس نمط تسرّب `data-t-fr`/`data-ph-fr` وعن نفس نمط XSS (باراميترات الرابط تُحقن في innerHTML) في كل صفحات المنصة الأخرى — لم يوجد أي مثال إضافي.

## متابعة: "أصلح ما امكنك اصلاحه" (22/07/2026)
1. **كلمات سر الأدمن أُعيد توليدها** — لأن النسخة السابقة (plaintext) لم تكن محفوظة في أي مكان قابل للاسترجاع. القيم الجديدة أُرسلت لـ Limam مرة واحدة في المحادثة، يجب حفظها في مدير كلمات سر فوراً.
2. **فجوة حقيقية أُصلحت في 3 لوحات التحكم** (`rizq_dashboard_store.html`, `rizq_dashboard_office.html`, `rizq_dashboard_corp.html`): زر "حفظ التغييرات" كان يكتب فقط في localStorage ولا يُزامن أبداً مع الخادم — أي تعديل يجريه التاجر (المدينة، الوصف، الصورة...) كان يبقى قديماً في دليل المعارض والصفحة العامة إلى الأبد. الآن `saveProfile()` في الثلاثة يستدعي `PATCH /api/accounts/mine/:id` بعد كل حفظ محلي.
3. **حقل "فئة النشاط التجاري" أُضيف لأول مرة** في نفس اللوحات الثلاث (كان موجوداً فقط في نموذج التسجيل الأولي) — أي حساب مسجَّل قبل اليوم يمكنه الآن ضبط فئته من لوحته مباشرة.
4. **تشغيل السيرفر الحي**: أُعيدت المحاولة (تثبيت نظيف كامل) — نفس القيد البيئي السابق (تثبيت `express` لا يكتمل ضمن حدود بيئة العزل هنا، مرة كانت `debug` ناقصة، هذه المرة `media-typer`). هذا ليس خللاً في الكود، ويحتاج تجربة حقيقية على جهاز Limam.
غير قابل للإصلاح من هذه البيئة: `ANTHROPIC_API_KEY` الحقيقي (يحتاج مفتاح Limam)، ومزامنة `BACKEND_SHARED_SECRET` إلى حقل لوحة الأدمن (يتطلب إدخاله يدوياً في متصفح Limam نفسه — تعمّدت عدم كتابته مباشرة في الملف حتى لا يتحول لسرّ مكشوف في صفحة ثابتة).

## ميزة المناقصات — قرار نهائي بشأن التسعير (23/07/2026)
بُنيت الميزة أول مرة بقرار "مجانية مؤقتاً" (22/07)، ثم صحّح Limam القرار صراحة في نفس اليوم التالي: **"لا العكس غير مجانية نهائيا بل هي الميزة الوحيدة التي ليس فيها اي تجربة لايام"** — أي أن المناقصة مدفوعة من اليوم الأول، وهي الميزة الوحيدة في كامل المنصة التي لا تُمنح أي فترة تجربة مجانية إطلاقاً (خلافاً لكل الباقات الأخرى التي تبدأ جميعها بـ3 أيام تجربة).
- `TENDER_REQUIRES_PACKAGE = true` في `server.js` (كان `false`)، و`TENDER_ACTIVE_STATUSES` أصبحت `['active','expiring_soon']` فقط (استُبعدت `trial`/`trial_expiring` عمداً، خلافاً لبقية أنظمة الباقات).
- **اكتشاف معماري مهم أثناء التنفيذ**: نظام `account-packages.json` (المستخدم لكل باقات المنصة) يحتفظ بسجل واحد فقط لكل `accountId` — لو استُخدم نفس المفتاح لباقة المناقصة، كان أي تجديد لاحق للباقة العامة لنفس التاجر (شهرية/ربعية/سنوية) سيمحو اشتراك المناقصة والعكس صحيح. الحل: مفتاح معزول `accountId + '::tender'` في كل نقاط القراءة/الكتابة (`hasTenderAccess`، `/api/tenders/package/activate`، `/api/tenders/package/status/:id`) — الآن يمكن للتاجر أن يملك باقته العامة + باقة المناقصة معاً دون تعارض.
- **سعر باقة المناقصة**: عندما سُئل Limam عن السعر، ترك القرار لي ("ما رايك انت"). حُدِّد سعر مبدئي **2500 أوقية/شهر** (بلا أي بند تجريبي في كتالوج الأدمين — عمداً، ليعكس القرار بصرياً)، قابل للتعديل فوراً من لوحة الأدمين (تبويب "إدارة الباقات" → فئة "باقة المناقصة") في أي وقت دون أي تعديل برمجي.
- تدفق الاشتراك الفعلي: التاجر يرى حالة اشتراكه في تبويب "غرفة المناقصات" بكل لوحة، يضغط "اشترك الآن" ← نفس نافذة الدفع/رفع الوصل الموجودة مسبقاً (`openPayModal`/`submitPayment`) بفئة `category:'tender'` ← يظهر الطلب في "طلبات الاشتراك" بلوحة الأدمين (بشارة زرقاء مميزة 📋) ← عند "تفعيل"، مسار مستقل (`activateTenderPackageForAccount`) يضرب `/api/tenders/package/activate` مباشرة بدل مسار الباقات العام، فلا يتعارض مع باقة الحساب الأخرى.
