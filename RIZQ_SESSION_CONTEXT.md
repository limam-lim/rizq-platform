# RIZQ — ملاحظات الجلسة القادمة
*آخر تحديث: 2026-08-06*

## تحديث 2026-08-06 (٤) — مراجعة وتصحيح صفحة الواجهة الرئيسية (rizq_landing_v8.html)
بدء جولة "خطوة خطوة" جديدة على الصفحات العامة (بعد إغلاق كل الداشبوردات) —
بنفس المنهجية البرمجية (مقارنة كل مفتاح مستخدَم فعلياً مقابل قاموس الترجمة
الحقيقي، فحص syntax لكل كتلة script، توازن `<div>`، دوال onclick).

**ملاحظة معمارية**: هذه الصفحة لا تحمّل `rizq_i18n.js`/`rizq_i18n_data.js`
إطلاقاً (نظام مستقل قديم بثلاث آليات ترجمة متوازية: `LANG[key]` عبر
`data-t`، نمط عكسي ذاتي التخزين المؤقت عبر `data-t-fr`/`data-ph-fr`، وعشرات
النصوص المباشرة `isAr?'...':'...'` داخل `toggleLang()`). **لم تُوحَّد على
المحرك المركزي في هذه الجولة** (حجم المخاطرة أعلى بكثير من الداشبوردات: دالة
`toggleLang()` وحدها تمتد لمئات الأسطر بمنطق مخصص كثيف) — اكتفيت بإصلاح
الأخطاء الحقيقية ضمن نفس البنية الحالية. يوجد أصلاً "shim" احتياطي (سطر
3306) من جلسة سابقة يحاكي `RizqI18n.t2()`/`getLang()` محلياً لأن الصفحة لا
تحمّل المكتبة الحقيقية — تحقّقتُ أنه يعمل بشكل سليم ويغطي كل الاستدعاءات.

**عطل حقيقي وُجد وأُصلح — نفس نمط "اتجاه واحد" المكتشَف سابقاً في داشبورد
الأفراد:** 16 مفتاح `data-t` مستخدَم فعلياً في الصفحة (شرائح الأقسام السريعة
`qc0`-`qc8` في الهيدر، وأزرار فلترة الإعلانات `f-all`/`f-re`/`f-car`/
`f-live`/`f-elec`/`f-truck`/`f-srv`) كانت موجودة في `LANG.fr` فقط وغائبة
تماماً من `LANG.ar` — يعني: التبديل للفرنسية يعمل بشكل صحيح، لكن الرجوع
للعربية يُبقي هذه العناصر الـ16 عالقة بالنص الفرنسي (لأن حلقة الترجمة تتحقق
`if (!t[key]) return;` وتتجاهلها بصمت). أُضيفت القيم العربية الصحيحة (مأخوذة
من النص الافتراضي الفعلي في الـHTML) لكل الـ16 مفتاحاً في `LANG.ar`.

**التحقق**: توازن `<div>` سليم 100% (926/926)، صفر أخطاء syntax على 6 كتل
script، صفر سمات `data-t` مكرَّرة، صفر دوال onclick غير معرَّفة، تغطية
100% الآن لكل مفتاح `data-t` مستخدَم فعلياً (ar+fr)، `rizq_health_check.js`
صفر أخطاء 🔴 على كامل المنصة.

## تحديث 2026-08-06 (٣) — توحيد الداشبوردات الأربعة على محرك RizqI18n (Limam: "نعم لتكون [لوحة الأدمن] هي مرجع")
تنفيذاً لموافقة صريحة على اقتراحي السابق: تمت إعادة هندسة نظام الترجمة في
الأربعة داشبوردات (أفراد/محلات/مكاتب/شركات) لتتوقف عن استخدام قواميس محلية
مكرَّرة (`T`, `_storeTranslations`, `_officeTranslations`, `_corpTranslations`
+ حلقة `[data-t]` يدوية في كل ملف) وتعتمد بدلاً منها على نفس محرك
`RizqI18n`/`rizq_i18n_data.js` الذي تستخدمه لوحة الأدمن فعلياً.

**ما تم بالضبط، لكل ملف:**
1. أُضيف `data-t-ns="dashboard"`/`"dashboard_store"`/`"dashboard_office"`/
   `"dashboard_corp"` على `<body>`.
2. كل قاموس محلي (T/_storeTranslations/_officeTranslations/_corpTranslations)
   نُقل بالكامل (ar+fr) إلى `rizq_i18n_data.js` كـ namespace مستقل مخصص لكل
   داشبورد (namespaces جديدة لم تكن موجودة: `dashboard_store`,
   `dashboard_office`, `dashboard_corp` — تفادياً لأي تصادم مع namespaces
   الصفحات العامة `store`/`office` الموجودة أصلاً بمفاتيح مختلفة تماماً).
   القاموس المحلي القديم حُذف من كل ملف HTML بعد النقل (صفر تكرار بيانات الآن).
3. سمة `data-t-ph` (غير قياسية، محلية الصنع في store/office/corp) استُبدلت
   بـ`data-ph` القياسية التي يفهمها `RizqI18n.applyStaticDom` تلقائياً.
4. دالة كل صفحة الخاصة بتبديل اللغة (`applyDashLang`/`_applyStoreLang`/
   `_applyOfficeLang`/`_applyCorpLang`) أُبقيت (السلوك الخاص بكل صفحة —
   استدعاءات render*()، تحديث عنوان التبويب النشط، نص زر اللغة المحلي —
   يبقى كما هو ولا يعرفه المحرك العام)، لكن حلقة `[data-t]`/`[data-t-ph]`
   اليدوية استُبدلت باستدعاء واحد `RizqI18n.apply(lang)` في بداية الدالة.
5. رزق_dashboard.html تحديداً: أزلتُ أيضاً دالة `toggleLangPage()` المحلية
   المكرَّرة (كانت بلا داعٍ — `RizqI18n` يوفّر توافقاً رجعياً بنفس الاسم عالمياً).

**عطل حقيقي خطير اكتُشف أثناء التوحيد في `rizq_dashboard.html` (لم يكن معروفاً
سابقاً، غير متعلق مباشرة بالترجمة):** متغيّر `isAr` كان يُستخدَم في 22 موضعاً
عبر الملف (تأكيد حذف إعلان، تنبيهات حفظ الملف الشخصي، تبديل إخفاء الهاتف،
التحقق من كلمة المرور، تأكيد حذف الحساب، نسخ رابط المشاركة) بينما تعريفه
الوحيد `let isAr = true` كان محصوراً داخل IIFE منفصل تماماً (كتلة `<script>`
أخرى) — أي أن كل هذه المواضع الـ22 كانت ستُطلق `ReferenceError: isAr is not
defined` فعلياً عند التنفيذ، معطِّلة تلك التفاعلات بالكامل. اكتُشف هذا فقط
لأن حذف القاموس المحلي استدعى حذف ذلك التعريف أيضاً فظهر الخلل للفحص
البرمجي. أُصلح باستبدال كل الـ22 موضعاً بـ`_rizqLang()==='ar'` (الدالة
الآمنة العالمية الموجودة أصلاً في الملف لهذا الغرض بالضبط، وفق تعليق سابق في
الكود نفسه: "لا تعتمد على المتغير isAr المحصور داخل IIFE منفصل").

**عطل مشابه أصغر في `rizq_dashboard_office.html`:** دالة `toggleServiceForm()`
كانت تقرأ `_officeTranslations` مباشرة (مستقلة عن `_applyOfficeLang`) لتحديث
نص زر طي/توسيع نموذج الخدمة — بعد حذف القاموس كانت ستُظهر نصاً عربياً ثابتاً
دائماً بغض النظر عن اللغة. أُصلحت لتقرأ من `RizqI18n.t()` مباشرة.

**التحقق (لكل الأربعة، مكرَّر آلياً وليس بصرياً):** توازن `<div>` سليم
100%، `node --check`/`new Function()` صفر أخطاء syntax على كل كتلة سكربت،
تغطية كاملة 100% لكل مفتاح `data-t`/`data-ph` مستخدَم فعلياً في كل HTML مقابل
القاموس المركزي الجديد (ar+fr معاً)، صفر إشارات متبقية لأسماء القواميس
المحلية القديمة، صفر دوال onclick غير معرَّفة، `rizq_health_check.js` صفر
أخطاء 🔴 على كامل المنصة.

**الحالة النهائية**: الخمسة (أفراد/محلات/مكاتب/شركات/أدمن) توحَّدت الآن على
نفس البنية المعمارية لمصدر ترجمة واحد (`rizq_i18n_data.js`) — النمط الذي
كان يسبب كل الثغرات المكتشَفة هذه الجلسة (حقل يُضاف للـHTML ويُنسى في
القاموس أو العكس) أصبح مستحيلاً بنيوياً الآن: أي إضافة مستقبلية تحتاج فقط
مفتاحاً واحداً في مكان واحد (`rizq_i18n_data.js`) بدل مكانين منفصلين.

## تحديث 2026-08-06 (٢) — تكملة "خطوة خطوة": داشبورد المكاتب + الشركات + لوحة الأدمن
نفس المنهجية البرمجية (مقارنة كل `data-t`/`data-t-ph` مستخدَم فعلياً مقابل قاموس
الترجمة الحقيقي لكل صفحة، ليس فحصاً بصرياً) — طُبِّقت على الثلاثة الباقية.

**`rizq_dashboard_office.html` — أُصلح:**
- قسم شريط جانبي كامل "🤖 الوكيل الذكي" (`sb-sec-ai` + `sb-agents`) كان بلا
  أي ترجمة في `_officeTranslations` — يبقى عربياً دائماً. أُضيف.
- `pf-video-ph` (placeholder رابط الفيديو التعريفي) و`prof-city-ph`
  (placeholder المدينة) غائبان تماماً — أُضيفا.
- **عطل مكرَّر من نفس نمط store/corp**: فقرة `data-t="p-promo-video-hint"`
  كانت نسخة زائدة/متبقية (يبدو أنها نُسخت من `rizq_dashboard_store.html` أثناء
  التطوير) مكرِّرة نفس شرح الفيديو التعريفي فوق الفقرة الصحيحة
  `prof-video-hint` المترجَمة أصلاً بشكل سليم — نص عربي زائد يبقى ظاهراً حتى
  بعد التبديل للفرنسية. حُذفت الفقرة الزائدة بدل ترجمتها (الفقرة الصحيحة
  الوحيدة كافية).

**`rizq_dashboard_corp.html` — أُصلح:**
- نفس عطل قسم "🤖 الوكيل الذكي" (`sb-sec-ai`/`sb-agents`) — أُضيف.
- نفس عطل الفقرة المكرَّرة الزائدة لشرح الفيديو (`p-promo-video-hint` مقابل
  `pf-promo-video-hint` الصحيحة) — حُذفت الزائدة، بنفس نمط office بالضبط.
- `pf-city-ph`, `pf-video-ph`, `mp-code-ph` (placeholder رمز التحويل البنكي)،
  و`an-total-inquiries`/`an-month-visits` (تسميتا بطاقتي إحصائيات في تبويب
  "الإحصائيات المتقدمة" — موجودتان في الـHTML لكن غائبتان كلياً من القاموس)
  — كلها أُضيفت (ar+fr).

**`rizq_admin.html` — لا يحتاج أي إصلاح، الوحيد من بين الأربعة الذي يستخدم
فعلياً نظام `RizqI18n` الموحّد الصحيح** (`<body data-t-ns="admin">` +
`rizq:langchange` مربوط بـ4 دوال إعادة رسم). تغطية 100% مؤكَّدة برمجياً —
0 مفتاح ناقص من أصل 314 (`data-t`+`data-ph`) في كلتا اللغتين. صفر سمات
data-t مكرَّرة، صفر دوال onclick غير معرَّفة من أصل 109 دالة مستخدَمة.
**يستحق أن يكون النموذج المرجعي** لو قرر Limam مستقبلاً توحيد الداشبوردات
الأربعة على نفس بنية `RizqI18n` بدل القواميس المحلية المنفصلة الأربعة
الحالية (كل واحدة منها كانت تحمل نفس نوع الثغرة: قسم أو حقل أُضيف للـHTML
ونُسي إضافته للقاموس).

**النتيجة الإجمالية**: صفر أخطاء 🔴 على كامل المنصة (`rizq_health_check.js`،
مُعاد تشغيله بعد كل إصلاح). كل الداشبوردات الخمسة (أفراد/محلات/مكاتب/شركات/
أدمن) + صفحتا المعارض العامتان مراجَعة ومُغلَقة الآن لهذا النمط من الأخطاء.

## تحديث 2026-08-06 — مراجعة مغلقة: داشبورد الأفراد + داشبورد المعارض/المحلات
تنفيذاً لطلب "أكّد إغلاق مراجعة الأفراد ثم راجع المعارض والمحلات" — تم فحص فعلي
(ليس قراءة كود فقط): `node --check` لكل كتلة JS، عدّ توازن `<div>`، ومقارنة برمجية
بين كل مفتاح `data-t`/`data-t-ph` المستخدم فعلياً في كل صفحة وقاموس ترجمتها
المحلي (سكربت Node مؤقت يستخرج القاموس ويقارنه، وليس فحصاً بصرياً).

**1) `rizq_dashboard.html` (الأفراد) — أُصلح:**
- لوحة "باقتي واشتراكي" (عنوان القسم، تجديد الاشتراك، زر الرفع، عنوان "باقات
  الاشتراك") كانت بلا أي ترجمة فرنسية إطلاقاً — 8 مفاتيح `data-t` موجودة في
  الـ HTML وغائبة كلياً من قاموس الصفحة المحلي `T` (لا AR ولا FR) — تبقى
  عربية دائماً بغض النظر عن اللغة المختارة. أُضيفت الترجمتان لكل المفاتيح.
- `search-mini-ph` (نص بحث "إعلاناتي") كان موجوداً في `T.fr` فقط وغائباً من
  `T.ar` — عيب اتجاه واحد: التبديل للفرنسية يعمل، لكن الرجوع للعربية يُبقي
  نص البحث فرنسياً عالقاً. أُضيف لـ`T.ar`.
- وسم `data-t="my-ads"`/`data-t="messages"` كان مكرَّراً حرفياً على نفس
  العنصر مرتين (HTML غير صالح، بلا أثر وظيفي حقيقي لكن نُظِّف).
- **ملاحظة معمارية (لم تُلمَس، توثيق فقط)**: الصفحة تملك نظامي ترجمة متوازيين
  — القاموس المحلي `T`/`applyDashLang` (هو الفعلي المُستخدَم) + نظام
  `RizqI18n`/`rizq_i18n_data.js["dashboard"]` (87 مفتاحاً معرَّفة، لكن ميتة
  تماماً هنا لأن `<body>` لا يحمل `data-t-ns="dashboard"` — خلافاً لـ
  `rizq_store.html`/`rizq_office.html`/`rizq_products.html`/`rizq_cart.html`
  التي تستخدم النظام الموحّد فعلياً). لا يوجد عطل مستخدم حالياً (القاموس
  المحلي يغطي كل شيء بعد الإصلاح)، لكنه ازدواجية كود يستحق توحيداً مستقبلياً
  لو أراد Limam ذلك (يخالف مبدأ "تجنّب تكرار الكود" المفضَّل).

**2) `rizq_dashboard_store.html` (يخدم المحلات والمعارض معاً — نفس لوحة
التحكم لكل حساب "محل"، بياناته تغذّي `rizq_showroom.html`/
`rizq_showroom_directory.html` العامّتين) — أُصلح:**
- `pf-video-ph` (placeholder حقل رابط الفيديو التعريفي للمحل) كان غائباً من
  قاموس `_storeTranslations` (AR وFR معاً) رغم استخدامه في الـ HTML —
  المفتاح الوحيد الناقص من أصل 167 مفتاحاً مستخدَماً (تغطية شبه كاملة أصلاً،
  بخلاف صفحة الأفراد). أُضيفت الترجمتان.
- الباقي (توازن `<div>`، syntax، دوال `onclick`) سليم 100% — لا أخطاء أخرى.

**3) `rizq_showroom.html` — سليمة تماماً**، لا تستخدم `data-t` إطلاقاً
(محتوى ديناميكي بالكامل عبر `_t()`/`renderCatalog()`)، مربوطة بحدث
`rizq:langchange` الموحّد بشكل صحيح.

**4) `rizq_showroom_directory.html` — عطل حقيقي أُصلح:** زر اللغة كان يحمل
`class="btn-lang"` (يُفعِّل مستمع `RizqI18n` الموحّد تلقائياً) **و**
`onclick="toggleLang()"` محلياً في آن واحد — نفس النمط الذي كان موثَّقاً
ومُصلَحاً سابقاً في صفحات أخرى (راجع تعليق 27/07 حول "تكرار التبديل مرتين
بنقرة واحدة"). كان هذا يُشغِّل منطقين مستقلين بنقرة واحدة (تنفيذ مزدوج
لإعادة الرسم + تضارب في نص الزر النهائي بين "ع" و"AR" حسب أيّ المعالجين
نُفِّذ أخيراً) رغم عدم كسره لاتجاه التبديل نفسه بالصدفة. أُزيل `onclick`
المحلي واستُبدل بمستمع `rizq:langchange` (نفس نمط `rizq_showroom.html`
الصحيح أصلاً).

**النتيجة الإجمالية**: صفر أخطاء `🔴` في `rizq_health_check.js` على كامل
المنصة بعد كل التعديلات (تحقُّق فعلي، أُعيد تشغيله بعد كل إصلاح).

## تحديث 2026-07-28 (٢) — إصلاح تسربات اللغة (cart/office/products) — بروتوكول جديد
تنفيذاً لتعليمة "واحدة تلو الأخرى + اختبار فعلي": بُني هارنس آلي (jsdom) يُشغّل
فعلياً سكربتات كل صفحة بـ rizq_lang=fr ثم يفحص شجرة DOM بحثاً عن أي نص عربي متبقٍ —
بدل الاكتفاء بقراءة الكود، هذا اختبار تنفيذي حقيقي.
1. **rizq_cart.html**: سطر التذييل الصغير في نافذة الإشعار القانوني
   ("Rizq Platform · موريتانيا...") بلا id وخارج قاموس أي آلية ترجمة — يبقى
   عربياً دائماً. أُصلح بإضافة data-t-fr (يلتقطها _cartBridge الموجود أصلاً).
2. **rizq_office.html**: 3 مشاكل — (أ) نفس خلل سطر تذييل الإشعار القانوني،
   (ب) `premium-badge-lbl` كان له data-t لكن المفتاح غائب كلياً من القاموس
   (فيتجاهله RizqI18n بصمت)، (ج) أداة "🧮 احسب تكلفة خدمتك" كاملة (عنوان،
   وصف، قوائم select، تسمية النتيجة، ملاحظة، زر الطلب) بلا أي ربط i18n إطلاقاً.
   أُضيفت 11 مفتاحاً جديداً (ar+fr) في rizq_i18n_data.js[office] + data-t على العناصر.
3. **rizq_products.html**: نفس خلل سطر تذييل الإشعار القانوني فقط. فُحص أيضاً
   `.product-name-fr` (8 عناصر بالعربية في وضع الفرنسية) — تبيّن أنه تصميم
   متعمّد (سطر ثانوي يعرض دائماً اللغة الأخرى) موجود بلا تغيير منذ نسخة
   الأرشيف 20260712 — ليس خللاً، لم يُلمَس.
جميع الثلاثة: صفر تسربات غير مقصودة بعد الإصلاح (تحقُّق فعلي عبر jsdom)، توازن
`<div>` سليم، صفر أخطاء syntax.

## فحص بداية الجلسة — 2026-07-28 (تنفيذاً للتعليمة الدائمة)
فحص شامل: syntax (`node -c` + `new Function` لكل كتلة `<script>`) على كل ملفات
HTML (22) وJS (24) والباك-إند — صفر أخطاء. توازن `<div>` — 100% في كل الملفات.
فحص onclick/فحص روابط `<script src>` المفقودة — لا شيء معطوب.

**خطأ حقيقي مكتشَف وأُصلح فوراً**: صفحة التسعير العامة `rizq_landing_v8.html`
(الفعلية المعروضة لكل زائر، وليس فقط كتالوج الأدمن الذي فُحص سابقاً) كانت
تحتوي على بند "📧 ردود ذكية على بريدك الإلكتروني/على البريد الإلكتروني" ضمن
مزايا الباقة الماسية المعروضة صراحة لزبائن المحلات (`DEF_STO`) والمكاتب
(`DEF_OFF`) — وعد تسويقي حقيقي مكسور، غير مبني إطلاقاً (نفس فجوة
`auto_reply_email` المُغلَقة تقنياً 27/07 لكن لم أفحص وقتها الصفحة العامة
نفسها، فقط كتالوج الأدمن). كذلك كان موجوداً في `PKG_DEFAULTS.general[diamond]`
في `rizq_admin.html` (فئة "عام" التي تُزامَن مع نفس مفتاح `rizq_packages` الذي
تقرأه صفحة التسعير كأولوية على `DEF_STO`/`DEF_OFF`). أُزيل من كلا الملفين مع
تعليق موثِّق. **هذا يعني: لو شاهد أي زائر حقيقي صفحة التسعير قبل هذا الإصلاح،
فقد رأى وعداً غير حقيقي — يستحق الانتباه له في أي تواصل تسويقي سابق.**

## تحديث 2026-07-27 — إغلاق فجوة "الباقة الماسية" (المكالمات + الويدجت)

## تحديث 2026-07-27 — إغلاق فجوة "الباقة الماسية" (المكالمات + الويدجت)
تنفيذاً لـ"اكمل المتبقي": تم فعلياً سدّ الفجوة التي كانت موثَّقة سابقاً (ميزات
`auto_reply_calls`/`ai_agent_full`/`vip_manager` غير محقَّقة من أي خادم):
1. **`rizq-backend/rizq_package_lifecycle_agent.js`**: أُضيفت `isDiamondActive(accountId)`
   (تتحقق من `account-packages.json` الحقيقي على الخادم) + نقطة API جديدة
   `POST /api/account-package/diamond-status-batch` (محمية بـ BACKEND_SHARED_SECRET).
2. **`rizq_admin.html`**: تبويب "وكلاء الباقة الماسية" أصبح يتطلب الآن اختيار
   حساب حقيقي (`sa-account-id`, قائمة مبنية من `RizqSub.hasFeature(id,'ai_agent_full')`)
   قبل الحفظ — لا يمكن بعد الآن تسجيل وكيل بلا ربط حقيقي بحساب مشترك فعلاً.
   القائمة تعرض أيضاً تحذيراً حياً لو انتهت باقة الحساب المرتبط.
3. **`rizq_call_handler.js`**: يفحص دورياً (كل 15 دقيقة) حالة كل مشترك مسجَّل
   عبر النقطة الجديدة، ولا يسمح بالرد الشخصي بشخصية المنشأة إلا لمن يملك
   فعلاً باقة ماسية نشطة من طرف الخادم — لا يمكن تزويرها من المتصفح.
   **يتطلب متغيرين جديدين في .env**: `RIZQ_BACKEND_URL` و`BACKEND_SHARED_SECRET`
   (يجب أن يطابق حرفياً نفس القيمة في .env الخاص بـ rizq-backend).
4. **`rizq_widget_embed.js`**: كانت المصادر رقم 2 و3 في `_loadPageProfile()`
   (صاحب متجر مسجَّل في نفس المتصفح / DOM scraping للزوار) تُخصِّص الويدجت
   باسم أي منشأة موافَق عليها بلا أي تحقق من الباقة — أُصلح بإضافة تحقق
   `RizqSub.hasFeature(id,'ai_agent_full')` فعلي قبل أي تخصيص.
5. **`rizq_secretary_agent.js`**: `_isDiamondActive()` كانت منطقاً محلياً
   مكرَّراً منفصلاً عن باقي المنصة — الآن تستخدم `RizqSub.hasFeature` كمصدر
   حقيقة أول (احتياط قديم لو تعذّر تحميل المحرك).
6. **`rizq_corp.html`**: كانت الصفحة الوحيدة (من بين store/office/corp) التي
   لا تحمّل `rizq_secretary_agent.js` إطلاقاً — أُضيف السطر الناقص.
7. **إعادة تصميم "مميزة" كتثبيت إعلان حقيقي (مهمة #219 — أُنجزت)**:
   اكتُشف أن الخلل كان أعمق من "غير مصمَّمة بشكل صحيح" فقط: شراء "مميزة" كان
   يمر عبر `activatePackage()` العامة، أي (أ) يكتب فوق `accounts[accId].package`
   بالكامل — فلو كان الحساب مشتركاً فعلاً بباقة شهرية نشطة، شراء تثبيت بـ500
   أوقية كان يُلغيها ويستبدلها بـ"مميزة" لمدة 3 أيام فقط، و(ب) حتى بدون هذا
   التعارض، `getPackageTier` كان يُصنّفها tier=1 فقط (لا تصل لـ vip_badge
   الذي يبدأ من tier=2) — فحتى شارة "مميز" والتثبيت الفعلي لم يكونا يتحققان!
   الحل: تخزين مستقل بمفتاح `adId` (`rizq_ad_boosts` عبر `RizqSub.activateAdBoost`/
   `isAdBoosted`)، منتقي إعلان جديد في `rizq_dashboard.html` (`openAdBoostPicker`)
   يفتح قبل نموذج الدفع، حقلا `adId`/`adTitle` جديدان في `submitSubscriptionRequest`،
   وفرع موافقة مستقل في `rizq_admin.html` (`activateAdBoostForRequest`) لا يمر
   إطلاقاً عبر `activateSubscriptionForAccount`. شارة/ترتيب "pin" في
   browse/search/listing أصبحت الآن `vip_badge الحسابية OR isAdBoosted(الإعلان)`.
8. **`auto_reply_email` — قرار نهائي 27/07/2026 ("افعل ما تراه مناسب")**:
   تحقّقت أولاً: كتالوج الباقات الحقيقي المعروض للزبائن (`PKG_DEFAULTS` في
   `rizq_admin.html`، باقات "ماسية") لا يَعِد أصلاً بهذه الميزة — فقط "رد
   تلقائي على المكالمات". لا يوجد وعد تسويقي مكسور لأي زبون حالياً. الميزة
   كانت موجودة فقط كعلم تقني (`TIER_FEATURES` مستوى 3 في `rizq_subscription_engine.js`)
   يشير لقدرة غير موجودة، وثلاث لوحات تحكم (store/office/corp) بها إشارات
   لعناصر DOM وهمية (`#btn-email-reply`) لم توجد يوماً في أي صفحة. القرار:
   أُزيل العلم `auto_reply_email` نهائياً من `TIER_FEATURES` (مع تعليق تفصيلي
   في الكود) + نُظّفت الإشارات الميتة في اللوحات الثلاث — بدل ترك قدرة وهمية
   قد يعتمد عليها كود مستقبلي بالخطأ. `rizq_email_handler.js` يبقى كما هو
   (يرد فقط على بريد الدعم العام لرزق). لو قرر Limam مستقبلاً بناء رد تلقائي
   حقيقي لكل مشترك على بريده الخاص، هذا يحتاج مشروعاً مستقلاً بالكامل: نظام
   ربط بريد المشترك (تسجيل + تحقق + تخزين آمن مشفَّر لبيانات IMAP/SMTP/OAuth)
   — لا يمكن تحقيقه بمجرد فحص باقة كما حدث مع المكالمات.

## تعليمة دائمة (كل جلسة جديدة)
في بداية أي جلسة عمل قادمة على هذا المشروع: **ابحث أولاً عن ثغرات/أخطاء بشكل عام على المنصة كلها** (ليس فقط الباقات — كل الملفات: صفحات HTML، محرّكات JS، الباك-إند) **قبل البدء بأي طلب جديد، وأصلحها مباشرة دون انتظار إذن** — تفويض عام سبق أن أعطاه المستخدم ("افعل ما تراه مناسب... أصلح كل شيء"). أُعيد تأكيد هذا صراحة في 2026-07-25: "البحث عن ثغرات بشكل عام على المنصة كلها" + "عند كل بداية جلسة".

## أولوية الجلسة القادمة: نظام الباقات والخدمات
المطلوب متابعة الربط الدقيق بين كل باقة والخدمات التي تدّعي أنها توفرها، بحيث لا تُفعَّل أي ميزة إلا لصاحب الباقة التي تشملها فعلاً. آخر إنجاز: تم تصحيح حد الإعلانات النشطة للحساب الفردي في `rizq_post.html` (1 لباقة "مجانية"، 10 لـ"باقة شهرية"، مع استثناء "مميزة" لأنها إضافة لكل إعلان وليست باقة حساب).

### الفجوات المعروفة والمفتوحة (لم تُحل بعد):
1. **ميزات نصية بحتة غير قابلة للبناء آلياً** (دعم مخصص، مدير حساب شخصي، تقارير PDF شهرية) — خدمات بشرية/عملية فعلاً، ليست أخطاء برمجية.
2. **ملاحظة معمارية بسيطة (ليست خطأ وظيفي، أولوية منخفضة)**: فئة "عام (الافتراضي)" في كتالوج الأدمن (`rizq_packages` في localStorage) غير مستخدمة إطلاقاً في أي تحقق حقيقي (`checkPostLimit` لا يستدعيها أبداً — فقط individual/store/office/corp). هي فقط احتياط عرض على صفحة التسعير العامة لقسم "المحلات" إن عدّل الأدمن تبويب "عام" تحديداً (نادر). لا حاجة للعمل عليها الآن.

### تدقيق features مقابل POST_LIMITS — أُنجز 28/07/2026 (٣):
تحقّق رقم برقم لكل فئة حقيقية مُفعَّلة فعلياً في `checkPostLimit`:
- **individual**: مجانية=1↔"إعلان واحد نشط" ✅ | باقة شهرية=10↔"حتى 10 إعلانات نشطة" ✅
- **store**: شهرية=50↔"عرض 50 منتج" ✅ | ربعية=200↔"عرض 200 منتج" ✅ | سنوية/ماسية=∞↔"غير محدود" ✅
- **office**: أساسية=5↔"حتى 5 عروض تأمين نشطة" ✅ | احترافية/شركات التأمين=∞↔"غير محدود" ✅
- **corp**: شهرية=30↔"عرض 30 إعلان" ✅ | ربعية/سنوية/ماسية=∞↔"غير محدود" ✅
**النتيجة: تطابق كامل 100%، لا يوجد أي وعد نصي مكسور في أي فئة مُفعَّلة فعلياً. لا حاجة لأي إصلاح.**

### مُنجَز (لا حاجة لإعادة العمل):
شارة verified/pin موصولة فعلياً بكل صفحات العرض (browse/search/listing/products/
showroom/corp/office/store) عبر `RizqSub.getBadges`+`isAdBoosted` — أُنجز 25-27/07.
"مميزة" أصبحت تدفق شراء حقيقي مستقل لكل إعلان (`activateAdBoost`) — أُنجز 27/07.
بوابة الباقة الماسية الحقيقية (مكالمات + ويدجت) — أُنجزت 27/07. راجع تحديثات
27/07 و28/07 أدناه للتفاصيل الكاملة قبل افتراض أي من هذا لا يزال مفتوحاً.

### مؤجَّل بانتظار سيرفر إنتاج فعلي (لا داعي للعمل عليه الآن):
- ضبط بيانات اعتماد حقيقية لـ WhatsApp/Twilio/Email في `rizq_package_lifecycle_agent.js`.

## سجلّ الفحص الشامل — 2026-07-25
تم تنفيذ فحص آلي شامل لكل ملفات HTML (22 ملفاً) وكل ملفات JS (26 ملفاً) والباك-إند:
- **صفر أخطاء syntax في JS** على كامل المنصة (تحقق عبر `node -c` وتشغيل كل كتلة `<script>` داخل `new Function`).
- **تم اكتشاف وإصلاح 13 خطأ حقيقي في توازن وسوم `</div>`** (وسوم إغلاق زائدة متبقية من نسخ ولصق قديم لنمط "نافذة الإشعار القانوني" و"نافذة الدفع/الباقات")، في: `rizq_cart.html`، `rizq_corp.html`، `rizq_dashboard_office.html`، `rizq_listing.html`، `rizq_office.html`، `rizq_post.html`، `rizq_products.html` (×2)، `rizq_search.html` (×2)، `rizq_store.html` (×2)، `rizq_landing_v8.html` (×2). كل الملفات الآن متوازنة 100%.
- **خطأ حقيقي وأخطر تم اكتشافه**: في `rizq_store.html` كان عنصر `<div class="fixed-disc-pill">` (زر الإشعار القانوني العائم) مبتوراً في منتصف قيمة `title` (كود HTML entity غير مكتمل، بلا `>` ولا نص ولا إغلاق) — هذا كان سيُفسِد العرض ويُحتمل أن يبتلع وسوم `<script>` التالية (rizq_navbar.js وrizq_messenger.js). تم إصلاحه بمطابقة النسخة الصحيحة الموجودة في باقي الصفحات.
- **حالات "غير متطابقة" تبيّن أنها غير حقيقية (false positives)** بعد تدقيق يدوي: فوارق `<span>` في `rizq_landing_v8.html` وrizq_listing.html — ناتجة فقط عن أن أداة الفحص السريعة تحسب كلمة "span" داخل تعليقات HTML توثيقية (`<!-- ... -->`) أو داخل نصوص قوالب JS (template strings)، وليست أخطاء حقيقية في العرض. لا حاجة لأي إصلاح هناك.
- لم يُفحص بعد في هذه الجولة: تدقيق شامل لتطابق كل onclick مع تعريف الدالة، وفحص الروابط الميتة (كان قد تم سابقاً في جلسات قديمة، يستحق إعادة فحص دوري).

## تصحيح ربط الباقات بالخدمات — 2026-07-25 (الجولة الثانية)
تنفيذاً لطلب صريح: "كل باقة يجب أن تقدّم الخدمات الموجودة بها فقط، وأي خدمة غير مبنية تُبنى وتُربط":

1. **حدود عددية دقيقة لكل باقة (بدل فحص ثنائي عام)** — أُضيف جدول `POST_LIMITS` + دالتا `getPostLimit()`/`checkPostLimit()` في `rizq_subscription_engine.js` (مصدر حقيقة واحد لكل الفئات). قبل هذا كانت `canPostNewAd()` تمنح "نشر غير محدود" لأي باقة نشطة أياً كان مستواها — فحساب بباقة "شهرية" (تَعِد بـ50 منتجاً فقط للمحل، 30 للشركة) كان يملك فعلياً نشراً غير محدود تماماً كصاحب الباقة السنوية. الآن مُطبَّق بدقة في: `rizq_dashboard_store.html` (50/200/غير محدود)، `rizq_dashboard_office.html` (5/غير محدود)، `rizq_dashboard_corp.html` (30/غير محدود)، `rizq_post.html` (أُعيد ربطه بنفس الدالة الموحّدة بدل منطقه المحلي المكرَّر).
2. **توضيح حاسم من Limam بخصوص "شارة موثّق"**: يوجد نوعان منفصلان تماماً —
   - **توثيق الهوية (verified)**: بطاقة هوية للأفراد / سجل تجاري للمحلات-المكاتب-الشركات — **مجاني دائماً** لكل حساب وافق عليه الأدمن، لا علاقة له بالباقة.
   - **شارة "مميّز برزق" (premium)**: ميزة مدفوعة صرفة، حصرية لمشتركي باقة تتضمن `vip_badge` (مستوى 2 فما فوق).
   نُفِّذ هذا عبر `RizqSub.getBadges(accId)` الجديدة (تُحسب حياً عند العرض، لا تُخزَّن كحقل ثابت — تختفي/تظهر فوراً مع الباقة). عُدِّل نص كتالوج `PKG_DEFAULTS` (rizq_admin.html) في كل الفئات لعدم خلط الاسمين. وُصِّلت فعلياً في `rizq_browse.html` (كانت `verified`/`pin` مثبَّتتين على `false` دائماً — بيانات وهمية 100% — والآن تُحسبان من حساب صاحب الإعلان الحقيقي؛ `pin` تستخدم نفس تسمية الواجهة الموجودة أصلاً "مميّز"/Vedette).
3. **متبقٍّ صراحة لم يُنفَّذ بعد** (يحتاج جولة قادمة):
   - وصل نفس شارتَي `verified`/`premium` في صفحات عرض المحلات/المكاتب/الشركات العامة (`rizq_products.html`، `rizq_showroom.html`، صفحات الشركة/المكتب العامة) — نُفِّذ فقط في `rizq_browse.html` حتى الآن.
   - "priority_listing" (الأولوية في ترتيب نتائج البحث الفعلي، لا فقط شارة العرض) لم تُربط بعد في منطق الفرز الحقيقي بكل صفحات التصفح.
   - ميزات الدايموند/الماسي (`auto_reply_calls`, `auto_reply_email`, `ai_agent_full`) — لم يُتحقَّق بعد هل `rizq_call_handler.js`/`rizq_email_handler.js` تتحقق فعلياً من هذه الميزة قبل التفعيل لكل حساب.
   - إعادة تصميم "مميزة" (تثبيت فردي لكل إعلان بمقابل 500 MRU) كتدفق شراء حقيقي مستقل — لا تزال غير مبنية كميزة قائمة بذاتها.
   - ميزات نصية بحتة غير قابلة للبناء آلياً (دعم مخصص، مدير حساب شخصي، تقارير PDF شهرية) — هذه خدمات بشرية/عملية، ليست أخطاء برمجية، ولا تدخل ضمن "الثغرات" المطلوب سدّها آلياً.

## المرجع التقني السريع
- محرك الاشتراكات المشترك: `rizq_subscription_engine.js` (`RizqSub`) — `checkSubscription`, `canPostNewAd`, `getPackageTier`, `TIER_FEATURES`, `FREE_FEATURES`, `PKG_LISTS`.
- الكتالوج المرجعي الحقيقي للباقات: `rizq_admin.html` → `PKG_DEFAULTS` (6 فئات: general/individual/office/store/corp/tender).
- وكيل دورة حياة الاشتراك (تذكيرات + تعليق تلقائي بعد 24 ساعة سماح): `rizq-backend/rizq_package_lifecycle_agent.js`.
- نمط الفحص المعتمَد بعد كل تعديل: استخراج كتل `<script>` وتشغيل `new Function(s)` لكل كتلة (فحص syntax)، وعدّ `<div\b` مقابل `</div>` (توازن الوسوم) — عدة ملفات لديها فروقات طفيفة قديمة غير مرتبطة بالتعديلات (مثال: `rizq_post.html` 193/194) — هذه مُوثَّقة وليست أخطاء جديدة.
