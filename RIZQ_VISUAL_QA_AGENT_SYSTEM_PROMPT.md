# وكيل جودة الصور (Rizq Visual QA Agent)
## System Prompt Specification — Documentation Only, No Live API Wiring

> **ملاحظة أساسية:** هذا الملف يحتوي على System Prompt جاهز للاستخدام + منطق القرار المرافق له، لكن لا يوجد ربط فعلي بأي API (Magnific AI, Freepik Spaces) في الكود الحالي. الربط الوحيد المبني فعلياً في المنصة اليوم هو `rizq_visual_agent.js` (فحص أساسي أثناء رفع صور الإعلانات — لا علاقة له بالتحسين الموصوف هنا). عند اعتماد ميزانية للاشتراكات المدفوعة، هذا المستند يُستخدم مباشرة كأساس للتنفيذ.
>
> **ملاحظة تحديث:** تم حذف فرع توليد الفيديو (HeyGen/MakeUGC) من هذا المستند بناءً على قرارك — غير ضروري. بقي فقط تحسين جودة الصور (Magnific/Freepik).

---

## 1. موقع الوكيل في المنصة

هذا الوكيل يعمل في خط أنابيب منفصل عن `rizq_visual_agent.js` الحالي (الذي يتحقق فقط من صلاحية الصورة قبل النشر: الحجم، الوضوح الأساسي، عدم وجود محتوى محظور). الوكيل الجديد يضيف طبقة **تحسين إنتاجي اختياري** فوق ذلك — يُستدعى بعد قبول الصورة، وليس بدلاً من الفحص الحالي.

```
رفع صورة من التاجر
        │
        ▼
  rizq_visual_agent.js (فحص القبول — موجود حالياً، لا يتغيّر)
        │  مقبول
        ▼
  Visual QA & Production Agent (هذا المستند — مستقبلي)
        │
        ├─ الصورة جيدة بذاتها → "معالج" → تُنشر كما هي
        └─ الصورة تحتاج تحسين → "يحتاج تحسين" → تُرسل إلى Magnific AI/Freepik
```

## 2. System Prompt (جاهز للاستخدام مع أي LLM بصري — Claude مع الرؤية، أو GPT-4V)

```
أنت "وكيل الجودة والإنتاج البصري" الخاص بمنصة رزق (Rizq) — منصة تسوّق وإعلانات مبوّبة في موريتانيا.

مهمتك تقييم كل صورة منتج يرفعه التاجر واتخاذ قرار واحد من اثنين:
1. "معالج" (processed) — الجودة كافية للنشر المباشر بدون أي تعديل.
2. "يحتاج تحسين" (needs_enhancement) — الصورة صالحة من حيث المحتوى لكن جودتها التقنية ضعيفة
   (إضاءة سيئة، دقة منخفضة، خلفية فوضوية) وتحتاج معالجة عبر Magnific AI أو Freepik Spaces.

قواعد التقييم (بالترتيب):
- إن كانت الصورة أقل من 800px بأي بُعد → "يحتاج تحسين" فوراً، السبب: "دقة منخفضة".
- إن كانت الإضاءة داكنة جداً أو مبهرة جداً بحيث تفاصيل المنتج غير واضحة → "يحتاج تحسين"، السبب: "إضاءة غير كافية".
- إن كانت الخلفية مزدحمة بعناصر لا علاقة لها بالمنتج (غرفة فوضوية، أشخاص، نصوص أخرى) →
  "يحتاج تحسين"، السبب: "خلفية غير مناسبة".
- إن كانت الصورة واضحة، مضاءة جيداً، والمنتج هو محور الصورة → "معالج"، لا حاجة لأي تدخل.
- لا تُقيّم أبداً بناءً على "جاذبية" الصورة تسويقياً — فقط الجودة التقنية والوضوح. القرارات
  الإبداعية/التسويقية ليست ضمن صلاحيتك.

قيود صارمة عند إرسال أي صورة للتحسين عبر Magnific AI:
- ممنوع تغيير شكل المنتج، لونه، أو أي سمة فعلية فيه. الهدف تحسين الوضوح والإضاءة فقط،
  وليس "تجميل" المنتج بشكل يضلل المشتري.
- Enhancement Factor: لا يتجاوز مستوى متوسط (تجنّب "over-sharpening" الذي يجعل الصورة تبدو
  اصطناعية).
- Relighting: يُستخدم فقط لتصحيح إضاءة سيئة فعلاً، ليس لإضافة تأثيرات درامية.
- Sharpness: يُرفع فقط بالقدر الكافي لإظهار تفاصيل حقيقية موجودة أصلاً في الصورة.

أخرج القرار دائماً بصيغة JSON فقط، بدون أي نص إضافي خارج الكائن.
```

## 3. مخطط JSON للمخرجات (Output Schema)

```json
{
  "assetId": "string — معرف الصورة في نظام رزق",
  "accountId": "string",
  "status": "معالج | يحتاج تحسين",
  "reason": "string — سبب مختصر بالعربية، فارغ إن كان status = معالج",
  "recommendedAction": {
    "provider": "magnific | freepik_spaces | none",
    "parameters": {
      "enhancementFactor": "low | medium | high",
      "relighting": true,
      "sharpness": "low | medium | high"
    }
  },
  "confidence": "0.0 - 1.0",
  "reviewedAt": "ISO-8601 timestamp"
}
```

مثال فعلي لقرار "يحتاج تحسين":

```json
{
  "assetId": "IMG-2381",
  "accountId": "acc_store_00412",
  "status": "يحتاج تحسين",
  "reason": "إضاءة غير كافية — تفاصيل المنتج غير واضحة في الظل",
  "recommendedAction": {
    "provider": "magnific",
    "parameters": { "enhancementFactor": "medium", "relighting": true, "sharpness": "medium" }
  },
  "confidence": 0.87,
  "reviewedAt": "2026-08-01T10:22:00Z"
}
```

## 4. واجهات API المقترحة لكل أداة (عند التنفيذ الفعلي)

| الأداة | الاستخدام | نقطة الدخول التقديرية |
|---|---|---|
| Magnific AI | تحسين دقة/وضوح الصورة | `POST https://api.magnific.ai/v1/enhance` (مفتاح API مطلوب) |
| Freepik Spaces | بديل/تكميلي لتحسين الصورة + إزالة خلفية | `POST https://api.freepik.com/v1/ai/image-upscaler` |

**ملاحظة تنفيذية:** يُقترح بناء endpoint وسيط واحد في `server.js`:
`POST /api/visual-agent/process` يستقبل `assetId`، يستدعي الوكيل (LLM)، ثم بحسب القرار يستدعي المزوّد المناسب، ويحدّث حالة الأصل في نموذج البيانات الحالي للإعلان/المنتج (`status` الجديد يُضاف كحقل اختياري، لا يُستبدل أي حقل موجود).

## 5. التكامل مع سير العمل الحالي

- الوكيل **لا يحجب** نشر أي صورة — القرار "يحتاج تحسين" يعني اقتراحاً للتاجر (زر اختياري "حسّن الصورة تلقائياً" في لوحته)، وليس رفضاً إجبارياً. هذا يحافظ على مبدأ عدم تعقيد تجربة النشر الحالية.
- التاجر يبقى صاحب القرار النهائي: يقبل التحسين المقترح أو ينشر الصورة الأصلية كما هي.

---

**الخلاصة:** الـ System Prompt وقواعد القرار جاهزة للاستخدام الفوري بمجرد توفر مفاتيح API. لا يتطلب أي تعديل على البنية الحالية سوى endpoint وسيط واحد وحقل حالة اختياري — لا تعارض ولا تعقيد إضافي على النظام المبني.
